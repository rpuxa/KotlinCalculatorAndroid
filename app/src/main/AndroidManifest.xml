<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="ru.rpuxa.kotlincalculatorandroid">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AppCompat.NoActionBar">
        <activity android:name="ru.rpuxa.kotlincalculatorandroid.activities.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-->
        Все активити
        <!-->
        <activity android:name=".activities.GraphActivity"/>
        <activity android:name=".activities.FunctionsActivity" />
        <activity android:name=".activities.ConstantActivity" />
        <activity android:name=".activities.SettingsActivity" />
        <activity android:name=".activities.settings.NumberFormatActivity" />
        <activity android:name=".activities.settings.SettingsGraphActivity" />
        <activity android:name=".activities.TutorialActivity" />



    </application>

</manifest>